use nalgebra::DMatrix;
use num::complex::Complex64;
// use super::*;

pub type CMatrix2 = DMatrix<Complex64>;

// fn get_unitary(hamiltonian:Array2<CArray2>,parameter:f64)->Array2<CArray2>{
    
// }

#[cfg(test)]
mod test_sim{

    // use super::*;

    // #[test]
    // fn nalgebra_test(){
    //     let complex_number = Complex::new(2.0, 3.0);

    //     // 2x2 の複素数行列を作成
    //     let complex_matrix: DMatrix<Complex<f64>> = DMatrix::from_row_slice(2, 2, &[Complex::new(1.0, 2.0), Complex::new(3.0, 4.0)]);
    
    //     // 複素数と行列の積を計算
    //     let result = complex_matrix * complex_number;
    
    //     println!("結果:\n{}", result);
    // }
}